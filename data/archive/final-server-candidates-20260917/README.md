# 服务器最终独有研究候选归档

时间：2026-09-17，Asia/Shanghai。保存旧服务器独有的财务事实、计算底稿、研究草稿、公司计算脚本与筛选实验结果。这里不是正式报告入口，不触发公开报告发布。

`manifest.json` 记录内容 SHA-256、所有原路径、原 schema、原状态及归类；`objects/` 按内容哈希保存原字节并保留扩展名。历史 complete/success 不等于本次重新财务验收。模型版本、路径与评分系数均为历史背景，不是未来默认配置或已验证投资规则。

另见 `../final-server-checks-20260917/manifest.json`：临时目录里 3 份独有的九号公司/博源校验草稿也已保留。原始财报、行情缓存、重复预检包装和原始运行 trace 不在本批长期成果中。

恢复时从 manifest 查找公司和原任务，验证对象 SHA-256 后复制到新的隔离工作区；不覆盖当前正式结果，不直接执行历史脚本。来源与期间索引及下载经验见私有 `myvyang/server-service-recovery` 的 `receipts/source-period-index-20260917.jsonl.gz` 与沉淀清单。

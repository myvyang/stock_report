已完成本次复核，并只写入本次 run_dir 的 `outputs/` 和 `work/`：

- `outputs/result.json`
- `outputs/report.md`
- `outputs/trace.txt`
- `work/market_price.json`

已重新读取 `result.json` 做程序校验：资产明细合计、负债明细合计、四类汇总、净额权益桥、毛利公式全部为 `0.00` 差异。

本次状态为 `review_required`，原因是年报中的 `监管商品款项` 为 263.15 亿元，超过总资产 5%，但财报只列示科目并说明 2025 年收入确认核算调整，未披露足够业务合同细节。

`pdftotext` 本机不可用，已按要求改用 Python `pypdf` 从本地年报 PDF 定向抽取文本核对。股价已通过指定 `price_skill` 获取并保存。

来源：
- 年报： https://static.cninfo.com.cn/finalpage/2026-04-30/1225273091.PDF
- 行情： https://qt.gtimg.cn/q=sz000858
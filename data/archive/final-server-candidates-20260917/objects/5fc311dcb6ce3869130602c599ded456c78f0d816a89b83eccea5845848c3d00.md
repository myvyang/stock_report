Use the stock-research skill and continue the same Triangle Tyre run.

The files pass structural validation, but the human semantic review found two issues. Correct the outputs without pretending the missing facts exist.

1. The latest canonical ideal table 2 contains only the old combined `沉淀资金` line. Your valuation adjustment estimates a 9.92亿元 operating cash buffer, but the displayed ideal table 2 does not explicitly show `经营必需资金` and the remaining `沉淀资金`. Either display a clearly labelled v3 estimate split that still closes to the canonical combined amount, with the assumption and source recorded, or set `ideal_statements.required_funds_separated=false`. Do not claim a split that the table does not show.
2. The 2021–2024 rows are operating-cash-flow-minus-capex proxies, not actual-history rows built from annual ideal statements. They are useful cross-checks but do not satisfy the result contract's actual-history requirement. Keep them in the human report as proxies, but do not mislabel them as fully aligned actual `G` history. Since the project currently has only a 2025 complete ideal statement for this company, set `data_quality.status=partial` and give explicit `incomplete_reasons`, unless you truly reconstruct and source the missing annual ideal-statement components.

Keep the calculated A/G estimate visible and clearly label its uncertainty. Re-run `compute-owner-value` and `validate`; partial is acceptable and honest. Update `report.md`, `result.json`, and `trace.txt`. Do not change protocol code or source inputs.

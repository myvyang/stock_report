# 服务器旧研究工作区恢复

2026-09-17，Asia/Shanghai。从服务器 `stock_report` 工作区取得未提交的旧研究成果，原基线为 `69735444a106bdefbf88bd586fd8316c8b1d43a6`。逐文件与当时 GitHub origin 历史比对，保留 45 个路径、23 种不同内容；版本目录中的重复内容保留原位置关系。

涉及鲁泰A、谭木匠、新天然气、九号公司。这里是历史工作区快照，不代表通过当前双表验收，也不替代 `data/analysis/` 或现行双表结果。原始字节不作财务修订。

`manifest.json` 保存原路径、工作区状态、原基线、大小、SHA-256 与 Git blob 身份。JSON 已解析验证，文件已按 SHA-256 复验，并检查常见凭证格式；检查不代表财务准确性复核。

运行 trace 与 SQLite 不进入本归档。trace 已离机私有保存；SQLite 仍留服务器，须按数据库一致性备份另行处理。其他旧运行目录中已存在于 GitHub 历史的相同内容不重复复制。

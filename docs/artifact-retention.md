# 成果保留与服务器退出

2026-09-17用户确认：GitHub保存获取方法、解析经验、来源记录及历史完整分析成果；公开可重新取得的原始财报默认作为缓存。此规则替代旧文档要求新增原始PDF长期入库的默认做法，既有历史Git对象不重写。

- 长期保存完整结果JSON、报告正文、版本身份、校验结果和必要来源说明。网页隐藏的小项仍保留在底稿。
- 来源至少记录官方URL、报告名称、期间、定位信息；已知原件哈希一起保留。
- 下载、解析、恢复方法保存在产出代码所属仓库；本仓不复制上游实现。
- 原始PDF和可重复提取全文可在确认来源可再获取、无活动消费者后清理。官方已撤回、不可重取或受限材料保留到单独决策。
- 运行环境、依赖下载缓存可重建；候选结果和trace不等于缓存，先分类再决定保留期。
- 密钥、Cookie、SSH私钥和访问凭证使用独立安全备份，不进入GitHub。
- 删除服务器成果前，核对文件哈希、Git提交和远端分支，不能只确认网页能打开。

本次双表完整原件见`data/archive/company_two_table/`。这是一次归档，不表示生产流水线已增加自动向本仓写入完整结果的步骤；后续仍需将完整结果归档与网页发布分别验收。
